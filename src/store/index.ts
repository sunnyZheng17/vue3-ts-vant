import { createStore } from 'vuex'

import ToDoList from "./Todo/index"
import RootFontSize from "./RootFontSize/index"

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    ToDoList,
    RootFontSize
  }
})
